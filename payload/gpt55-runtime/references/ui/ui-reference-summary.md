# UI Reference Summary

Use for UI/design routes.

Principles:

- match the product domain;
- avoid generic template feel;
- prefer existing design system;
- validate text overflow, responsive behavior, and accessibility;
- use real visual references only when needed;
- visually inspect final UI when possible.

Large visual curadorias remain preserved in the source archive and should be searched only when a UI route needs them.

