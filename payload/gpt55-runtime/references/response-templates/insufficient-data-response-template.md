# Insufficient Data Response Template

Use when the user asks for a conclusion that the available data cannot support.

Required:

- state insufficient basis;
- list missing data;
- avoid definitive conclusion;
- propose next data/source/validation step.

