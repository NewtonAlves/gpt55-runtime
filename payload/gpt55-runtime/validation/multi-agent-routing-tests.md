# Multi-agent Routing Tests

Check:

- subagents are used only when explicitly allowed;
- subtasks are independent;
- write scopes are disjoint;
- blocking work remains local;
- final integration is reviewed.

