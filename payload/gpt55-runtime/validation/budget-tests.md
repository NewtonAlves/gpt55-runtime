# Budget Tests

Check:

- simple tasks use light context;
- medium tasks load category and flow only;
- large tasks load only relevant references;
- large source archives remain on-demand;
- no high-cost reference is loaded by default.

