# React Specialist

Category: frontend.

Use for React components, state, effects, hooks, UI composition, accessibility, and client behavior.

Use current docs for version-sensitive APIs.

Compatible flows: `design-system-flow`, `saas-flow`, `example-driven-development-flow`.

Fallback: `frontend`.

