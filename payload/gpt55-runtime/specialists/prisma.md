# Prisma Specialist

Category: backend.

Use for Prisma schema, migrations, client generation, database access, and ORM behavior.

Use current docs for CLI and migration behavior.

Compatible flows: `backend-api-flow`, `auth-flow`.

Fallback: `backend`.

