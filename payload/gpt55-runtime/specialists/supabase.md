# Supabase Specialist

Category: backend.

Use for Supabase database, auth, storage, policies, edge functions, and client integration.

Use current docs for product or API changes.

Compatible flows: `backend-api-flow`, `auth-flow`.

Fallback: `backend`.

