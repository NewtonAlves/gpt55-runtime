# Memory Policy

Use memory when prior project choices, user preferences, or workspace history may change the route.

Do not load memory for trivial self-contained tasks.

If memory-derived facts may be stale and verification is cheap, verify. If not verified, say that the fact came from prior memory and may be stale.

After long work, summarize decisions, files changed, validation, and remaining risks.

