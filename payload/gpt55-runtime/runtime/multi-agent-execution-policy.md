# Multi-agent Execution Policy

Use subagents only when the user explicitly allows subagents, delegation, or parallel agent work.

Use them for independent research, disjoint code changes, or validation that can run in parallel.

Do not delegate the immediate blocking task when local execution is faster or more reliable.

