# Routing Philosophy

Use specialists to reduce ambiguity, not to create ceremony.

Use flows to make repeated work predictable.

Use references to supply detail without bloating the active context.

Use validation to prevent confident but unsupported conclusions.

Use disambiguation to reduce uncertainty before routing. A short request can be valid, but a vague request must not trigger implementation before objective, platform, scope, and output are understood.
